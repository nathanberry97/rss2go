<!DOCTYPE html>
<html lang="en">
<head>
    {{ .metadata }}
</head>

<body>
    <header>
        {{ .navbar }}
    </header>

    <main class=articles>
        <h1 class=articles__header>{{ .title }}</h1>

        {{ .query }}

        <button onclick="toTop()" id="toTopBtn" class="btn__to-top">↑</button>
    </main>
</body>
</html>
